# Security Notes

## Secrets

Do not commit secrets, passwords, tokens, API keys, or `.env` files to the repository.

Use `.env.example` to document expected environment variables.

## Dependency Scanning

The team will add dependency scanning later in the semester.

## Container Scanning

The team will add container scanning later in the semester.

## GitHub Actions Permissions

The team should use least-privilege workflow permissions when possible.
